import { PostgresDialect as PGDImport } from './Postgres';
export type PostgresDialect = PGDImport;

type Dialects = PostgresDialect; // Placeholder for future Dialects as a union type
export default Dialects;

